const path = '../../assets/sounds';

export const buttonPress = new Audio(`${path}/button-press.wav`);

export const timer = new Audio(`${path}/kitchen-timer.mp3`);

export const forest = new Audio(`${path}/forest-sound.wav`);
forest.loop = true;

export const rain = new Audio(`${path}/rain-sound.wav`);
rain.loop = true;

export const coffeeShop = new Audio(`${path}/coffee-shop-sound.wav`);
coffeeShop.loop = true;

export const fireplace = new Audio(`${path}/fireplace-sound.wav`);
fireplace.loop = true;
